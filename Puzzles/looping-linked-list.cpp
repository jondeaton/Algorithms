/*
 * File: looping-linked-list.cpp
 * -----------------------------
 * Algorithm to determine if there is a loop in a singly linked list
 */

template < class T>
T findStartOfLoop(Node<T> list) {
    Node* slow = &list;
    Node* fast = & list;

}

